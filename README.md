# 🔐 Password Generator (Full Stack Project - React + Spring Boot)

A modern and secure password generator web app built with **React (Frontend)** and **Spring Boot (Backend)**.  
It allows users to input their name and generates a strong, random password combining their name and randomized characters — ideal for learning **full-stack development**.

---

## 🚀-------------------Features-------------------

###-------------------Backend (Spring Boot)-------------------
- REST API built using Java and Spring Boot.
- Uses **SecureRandom** for password generation.
- Returns generated password as JSON.
- Simple and clean service-controller architecture.

###-------------------Frontend (React)-------------------
- Modern responsive UI.
- Takes user's name as input.
- Sends the name to backend via API call (Axios).
- Displays generated password dynamically.
- 3D dark-themed animated background using React + CSS.

---

